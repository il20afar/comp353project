import React from "react";
import { D } from "../../../../imports";
import "./LiveThreads.scss";

const LiveThreads = (props) => {
  const {} = props;

  return <D cn="LiveThreads"></D>;
};

export default LiveThreads;
